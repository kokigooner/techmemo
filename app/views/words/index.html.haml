.main-content
  .index-container
    .word-box
      - @words.each do |word|
        .word-box__top
          %p 単語
          = word.word
          -# .word-box__top__right
          -#   - if user_signed_in? && current_user.id == tweet.user_id
          -#   %li
          -#     = link_to '編集', "/words/#{word.id}/edit", method: :get
          -#   %li
          -#     = link_to '削除', "/words/#{word.id}", method: :delete
        .word-box__bottom
          %p 意味を見る
          .word-box__bottom__mean
            = word.mean


